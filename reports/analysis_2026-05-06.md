The report file write needs your approval — here is the completed report as text (the `run_tests.sh` script can capture it, or you can approve the write):

---

# CSC Test Analysis — 2026-05-06

## Summary

| Passed | Failed | Skipped | Total |
|--------|--------|---------|-------|
| 68     | 0      | 4       | 72    |

**vs. previous run (2026-05-05):** 0 failures this run (down from 2 test_bug failures). `test_10_4_cancellation_policy_info_present` is now passing — previously a skip. The two formerly-failing Stripe tests (`test_8_1`, `test_8_2`) are now gracefully skipping instead of erroring, which is an improvement in stability. One new skip appeared: `test_4_5_remove_from_cart`.

---

## Real Bugs _(app needs fixing)_

None — zero real_bug failures this run.

---

## Test Bugs _(test needs updating, not the app)_

None.

---

## Missing Data / Skips

### Cart state not preserved into remove-from-cart test

- **Affected tests:** `tests/test_04_booking_individual.py::test_4_5_remove_from_cart`
- **Description:** Test skipped with "No items in cart to remove." The test depends on an item already being in the cart when it runs, but by that point the cart is empty. The preceding test (`test_4_4_cart_timer_visible`) adds a room to cart but does not guarantee that item survives into `test_4_5`. This is a test isolation gap — tests that need cart state should each add their own item, or use a shared fixture that guarantees cart state at entry.
- **Gap:** The cart remove flow (Section 4, test 4.5) is untested. Removing items from cart before checkout is a critical pre-booking action.

### Stripe payment tests — no room in cart

- **Affected tests:** `tests/test_07_09_payment.py::test_8_1_stripe_success_payment`, `tests/test_07_09_payment.py::test_8_2_stripe_declined_card`
- **Description:** Both tests skip with "Stripe card fields not present or cart empty ($0 total) — prior tests may have exhausted available rooms for these dates." The tests lack a cart pre-fill fixture and depend on room availability at hardcoded test dates. When Section 4 booking tests consume available rooms for those dates earlier in the suite, these tests arrive at an empty cart and skip gracefully. Note: these tests previously failed with a Stripe iframe strict mode violation — the graceful skip guard is an improvement, but the root gap (no dedicated cart pre-fill fixture) remains.
- **Gap:** End-to-end Stripe payment flows (success and decline) are untested. A reusable `cart_with_room` fixture using a dedicated non-contested future date range would isolate these tests from availability contention and unblock both.

### Single seeded notification consumed by bulk-mark test

- **Affected tests:** `tests/test_16_notifications.py::test_16_4_individual_notification_mark_read`
- **Description:** Test skips with: "`test_16_3` bulk-marks all notifications as read before this test runs, consuming the single seeded unread notification. Needs a second seeded unread notification to run reliably in the full suite." The `seed_test_data` command creates only one unread notification for Alice; `test_16_3` marks all as read, leaving nothing for `test_16_4` to act on individually.
- **Gap:** Per-notification mark-read functionality is untested. `seed_test_data` needs to seed at least two unread notifications so both tests have independent state.

---

## Sections Not Yet Written

- Section 2 — Registration / Onboarding
- Section 5 — Family Booking (needs family member test data)
- Section 6 — Guest Booking
- Section 12 — Admin Booking (needs admin credentials)
- Section 13 — Admin Manage (needs admin credentials)
- Section 15 — Invoicing (needs financial admin credentials)
- Section 22 — Cross-Browser (requires `playwright install firefox webkit`)
- Section 23 — Race Conditions (partial coverage via `test_20_6`; full coverage needs precise timing and Django shell access)

---

## Recommendations

1. **Fix cart isolation for Sections 4 and 7–9 (highest priority):** Add a reusable `cart_with_room` pytest fixture that logs in, searches availability for a dedicated non-contested date range, and adds a room to cart. Apply it to `test_4_5_remove_from_cart` and the two Stripe payment tests. This single fixture unblocks three skips and restores end-to-end payment coverage.

2. **Seed two unread notifications:** Update `seed_test_data` to create two unread notifications for Alice, so `test_16_3` and `test_16_4` each have independent state. Minimal seed change; restores individual mark-read coverage.

3. **Obtain and configure missing credentials:** Admin and financial admin accounts are the blocker for Sections 12, 13, and 15. Add them to `credentials.txt` and `helpers.py` to unlock ~15+ currently unwritten tests.

4. **Write Section 5 (Family Booking):** Requires family member test data in the seed. Once seeded, covers a core multi-person booking flow not yet tested at all.

5. **Write Section 2 (Registration / Onboarding):** No credentials required — covers the public-facing signup flow, currently entirely untested.

6. **Install cross-browser runtimes and write Section 22:** `playwright install firefox webkit` is a one-command setup; write the Section 22 tests to unlock cross-browser coverage.
